# auto-generated by grc.converter

id: dect2_packet_decoder
label: Packet decoder
category: '[DECT2]'

parameters:
-   id: rx_id
    label: Part RX ID
    dtype: int
    default: '0'
-   id: part_to_decode
    label: Part to Decode (0 - RFP, 1 - PP)
    dtype: int
    default: '1'

inputs:
-   domain: stream
    dtype: byte
-   domain: message
    id: rcvr_msg_in
    optional: true

outputs:
-   domain: stream
    dtype: byte
-   domain: message
    id: log_out
    optional: true

templates:
    imports: import dect2
    make: dect2.packet_decoder(${part_to_decode})
    callbacks:
    - select_rx_part(${rx_id})
    - select_part_to_decode(${part_to_decode})

file_format: 1
